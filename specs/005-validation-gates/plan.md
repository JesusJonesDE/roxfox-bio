# Implementation Plan: Computational Validation Gates

**Branch**: `005-validation-gates` | **Date**: 2026-05-28 | **Spec**: [spec.md](spec.md)

## Summary

Add four computational validation gates (ADMET, MM-GBSA rescoring, selectivity docking panel, MD pose stability) as a new `pipeline validate` command. Each gate produces a pass/fail decision and markdown report. A dashboard command aggregates results across scaffolds into a wet-lab handoff decision. All gates run locally on Apple M1 Max 64 GB. The MD gate uses a 2 ns implicit solvent fast mode by default (1 hour) due to OpenMM lacking a Metal GPU backend; a 50 ns full mode is available as an explicit opt-in.

---

## Technical Context

**Language/Version**: Python 3.11 (existing project)

**Primary Dependencies (new)**:
- `admet-ai` (pip) — ADMET gate
- `gmx_mmpbsa`, `gromacs`, `ambertools` (conda-forge, osx-arm64) — MM-GBSA gate
- `openmm`, `openmmforcefields`, `openff-toolkit` (conda-forge) — MD local preparation
- `pdbfixer` (conda-forge) — PDB preparation for MD
- `runpod` (pip) — MD cloud GPU dispatch (RunPod A100)

**Dependencies (existing, reused)**: `vina`, `meeko`, `rdkit`, `biopython`, `pandas`, `rich`, `httpx`, `typer`, `tenacity`

**Storage**: Files under `data/results/{gene}/` and `data/cache/{gene}/` (existing pattern). Gate results cached as JSON via existing `CacheManager`.

**Testing**: pytest (existing). Unit tests for gate logic; integration test using SCF-009 on VRK1.

**Target Platform**: macOS (Apple M1 Max, 64 GB). All computation local. No GPU acceleration for MD (OpenMM has no Metal backend).

**Performance Goals**:
- ADMET: < 60 s per scaffold
- MM-GBSA: < 10 min per scaffold (CPU)
- Selectivity panel: < 2 h per scaffold (4 × Vina docking)
- MD: < 2 h per scaffold (20 ns explicit solvent, HMR 4 fs timestep, RunPod community A100, ~$1–3/run, hard cap $5)

**Constraints**: No paid APIs. No account-required services. Offline-capable for all gates except initial structure downloads.

**Project Type**: CLI pipeline extension (follows existing `pipeline/stages/` pattern)

---

## Constitution Check

Constitution is a blank template — no project-specific constraints defined. Applying standard principles:

- ✅ Each gate is independently testable via `--gate <name>`
- ✅ Follows existing stage module pattern (`pipeline/stages/validate/`)
- ✅ Cache-first (existing CacheManager reused)
- ✅ No unnecessary abstractions — gates are simple functions
- ✅ Graceful degradation: gates return ERROR status instead of crashing

---

## Project Structure

### Documentation (this feature)

```text
specs/005-validation-gates/
├── plan.md              ← this file
├── spec.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── cli_commands.md
├── checklists/
│   └── requirements.md
└── tasks.md             ← generated by /speckit.tasks
```

### Source Code

```text
pipeline/
├── stages/
│   └── validate/
│       ├── __init__.py
│       ├── validate.py          # run_validate(), gate registry, dashboard, handoff report
│       └── gates/
│           ├── __init__.py
│           ├── admet.py         # Gate 1: ADMET-AI predictions
│           ├── mmgbsa.py        # Gate 2: gmx_MMPBSA rescoring
│           ├── selectivity.py   # Gate 3: off-target docking panel (reuses run_dock)
│           └── md.py            # Gate 4: OpenMM 2ns/50ns simulation
├── cli.py                       # Add `validate` command (existing file, extend)
└── models.py                    # Add GateResult, ValidationResult dataclasses

tests/
└── unit/
    ├── test_validate_admet.py
    ├── test_validate_mmgbsa.py
    ├── test_validate_selectivity.py
    └── test_validate_md.py

data/
└── results/
    └── {gene}/
        ├── validate_admet_{scaffold}.md
        ├── validate_mmgbsa_{scaffold}.md
        ├── validate_selectivity_{scaffold}.md
        ├── validate_md_{scaffold}.md
        ├── validate_md_{scaffold}_rmsd.csv
        ├── validation_result_{scaffold}.json
        ├── validation_dashboard.md
        ├── validation_dashboard.json
        └── wetlab_handoff_{scaffold}.md

data/
└── cache/
    └── shared/
        └── selectivity_panel/
            ├── EGFR_1M17.pdb
            ├── CDK2_1E9H.pdb
            ├── PLK1_2OKR.pdb
            └── VRK2_AF2.pdb
```

---

## Key Implementation Decisions

### Gate 1 — ADMET
- Tool: `admet-ai` (Chemprop v2 models, TDC benchmark)
- Input: SMILES string
- Output: dict of property scores
- Thresholds: BBB_Martini > 0.5, CYP1A2/2D6/3A4_Inhibitor < 0.3, Solubility logS > −4, HIA_Hou > 0.3
- No external calls at prediction time; model weights bundled with package

### Gate 2 — MM-GBSA
- Tool: `gmx_MMPBSA` (CPU, conda-forge osx-arm64)
- Forcefield: GAFF2 (small molecule) + ff19SB (protein)
- Input: receptor PDB + top pose PDBQT (converted to PDB via meeko/RDKit)
- Output: ΔG in kcal/mol
- Threshold: ΔG ≤ −7.0 kcal/mol
- GROMACS lacks Metal GPU support → CPU-only is fine for rescoring (2–5 min)

### Gate 3 — Selectivity
- Tool: existing `run_dock()` from `pipeline/stages/dock/dock.py`
- Panel: VRK2 (AF2), EGFR (1M17), CDK2 (1E9H), PLK1 (2OKR)
- Structures cached in `data/cache/shared/selectivity_panel/`
- Selectivity index = |target ΔG| / max(|off-target ΔG|); pass if SI ≥ 10
- VRK2 AlphaFold2 model downloaded from EBI API; flagged with low-confidence warning

### Gate 4 — MD (cloud only, RunPod A100)
- Tool: RunPod API (`runpod` pip package), community cloud A100
- Requires: `RUNPOD_API_KEY` environment variable
- Hard cost cap: `--md-max-cost` flag (default $5); pipeline estimates cost before submission and raises ERROR if exceeded
- RunPod job timeout: 90 minutes (hard kill); pipeline analyses available trajectory if hit
- Local preparation (M1 Max, ~5 min): PDB + top pose PDBQT → PDBFixer → OpenMM Modeller + GAFF2/ff14SB → HMR (4 fs timestep) → energy minimise → serialise OpenMM XML
- Cloud execution: submit XML to RunPod, poll for completion, download RMSD CSV + summary
- 20 ns explicit solvent (TIP3P, 10 Å shell), ff14SB + GAFF2, 4 fs timestep (HMR)
- ~$1–3 per run on community A100; completes in < 2 h
- Pass: mean ligand heavy-atom RMSD ≤ 3.0 Å over final 10 ns

### Dashboard
- Rich Table in terminal (existing Rich dependency)
- Markdown table written to `validation_dashboard.md`
- Handoff report auto-generated for any scaffold with all gates PASS

---

## Complexity Tracking

| Design choice | Why needed | Simpler alternative rejected because |
|---|---|---|
| Separate `gates/` subdirectory | 4 gates × different dependencies; isolation prevents import errors if one tool not installed | Flat files would mix concerns and make optional-dependency handling harder |
| Cloud-only MD via RunPod | OpenMM has no official Metal backend; community plugin is unmaintained (Aug 2024), pinned to OpenMM 8.1, source-build only; CPU is 0.5–2 ns/day. Cloud A100 gives ~500 ns/day with HMR at ~$1–3/run — simpler, faster, more reliable | Local CPU/Metal adds complexity and maintenance risk for marginal benefit; cloud is the production-grade path |
| HMR + 4 fs timestep | Doubles throughput at no accuracy cost for pose stability; 20 ns with HMR is equivalent to 40 ns at 2 fs for this use case | Standard 2 fs timestep would double cost and runtime with no scientific benefit for binary pass/fail |
| 20 ns instead of 50 ns | Pose drift occurs in first 5–10 ns; 20 ns is sufficient for go/no-go decision | 50 ns adds cost and time without changing the pass/fail outcome in the vast majority of cases |
| Hard $5 cost cap + 90 min timeout | Prevents runaway costs; a job that takes > 90 min indicates a problem worth investigating | No cap would make the gate unpredictable in cost |
| Reuse `run_dock()` for selectivity | Selectivity is exactly docking against different targets | Reimplementing docking would duplicate 200+ lines |
