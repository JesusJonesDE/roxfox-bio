name: rxpipeline
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - rdkit
  - pip
  - pip:
      - typer>=0.12
      - rich>=13
      - httpx>=0.27
      - tenacity>=8
      - pandas>=2
      - jinja2>=3
      - pytest>=8
